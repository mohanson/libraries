# Expvpp

Package expvpp provides some helper functions for Go's expvar package.

It is designed to make runtime metrics easier to expose and monitor with a simple average helper and a cleaner debug export endpoint.

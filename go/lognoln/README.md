# Lognoln

Package lognoln provides a small helper around the standard log package.

It prints log messages without adding an extra newline at the end, which is useful for interactive prompts and status output.
